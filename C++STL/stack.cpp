#include <bits/stdc++.h>
using namespace std;
int main()
{
  // STACK: FILO or LIFO
  stack<int> st;
  // pop
  // push,emplace
  // top
  // empty
  // size

  // delete entire stack:
  while (!st.empty())
  {
    st.pop();
  }
}